**input**
new_student_df = pd.DataFrame({
    "Study_Hours": [6],
    "Attendance": [72],
    "Project_Done": [1]
})

prediction = model.predict(
    new_student_df
)[0]

probabilities = model.predict_proba(
    new_student_df
)[0]

print(
    "Probability of Not Placed:",
    round(probabilities[0] * 100, 2),
    "%"
)

print(
    "Probability of Placed:",
    round(probabilities[1] * 100, 2),
    "%"
)

if prediction == 1:
    print("Naïve Bayes Prediction: Placed")
else:
    print("Naïve Bayes Prediction: Not Placed")

**    **ouput**
**    
Probability of Not Placed: 0.0 %
Probability of Placed: 100.0 %
Naïve Bayes Prediction: Placed
